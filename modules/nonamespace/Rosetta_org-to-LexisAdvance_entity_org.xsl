<?xml version="1.0" encoding="UTF-8"?>
<xsl:stylesheet xmlns:xsl="http://www.w3.org/1999/XSL/Transform" xmlns:dita="http://dita.oasis-open.org/architecture/2005/" xmlns:comm="http://www.lexis-nexis.com/glp/comm" xmlns:entity="http://www.lexisnexis.com/xmlschemas/content/shared/identified-entities/1/" version="2.0" exclude-result-prefixes="dita">

	<dita:topic xmlns="http://dita.oasis-open.org/architecture/2005/" id="Rosetta_org-to-LexisAdvance_entity_org">
  <title><sourcexml>org</sourcexml> to <targetxml>entity:org</targetxml> <lnpid>id-CCCC-10416</lnpid></title>
  <body>
    <section>
      <p><sourcexml>org</sourcexml> becomes <targetxml>entity:org</targetxml>.</p>
    </section>
    <example>
      <title>Source XML</title>
      <codeblock>

&lt;p&gt;
 &lt;text&gt;
  &lt;org&gt;Judges Chambers&lt;/org&gt;
 &lt;/text&gt;
&lt;/p&gt;

	</codeblock>
    </example>
    
    <example>
      <title>Target XML</title>
      <codeblock>

&lt;p&gt;
 &lt;text&gt;
  &lt;entity:org&gt;Judges Chambers&lt;/entity:org&gt;
 &lt;/text&gt;
&lt;/p&gt;

	</codeblock>
    </example>      
    <section>
      <title>Changes</title>
      <p>2013-03-14: <ph id="change_20130314_ab">Created.</ph></p>
    </section>
  <!--  <xsltinclusion>
      <xsl:stylesheet version="2.0">
        <!-\- BRT: the following template is in entity.xsl, match="org" in xsl:otherwise choice. I did not split this out in entity.xsl - left 'as is' -\->
        <xsl:template match="org">
          <xsl:element name="entity:org">
              <xsl:apply-templates/>
          </xsl:element>
        </xsl:template>
      </xsl:stylesheet>
    </xsltinclusion>-->
  </body>
	</dita:topic>

	<!--  @@@ This file has been autogenerated (although the DITA file already has embedded XSLT).  Remove this comment after manual development complete!  (also remove the embedded XSLT within the DITA above) @@@  -->
	<!--    Original DITA file location:  DITA\ConversionInstructions\Rosetta\common_newest\Rosetta_org-to-LexisAdvance_entity_org.dita  -->
	 

        <!-- BRT: the following template is in entity.xsl, match="org" in xsl:otherwise choice. I did not split this out in entity.xsl - left 'as is' -->
        <xsl:template match="org">
          <xsl:element name="entity:org">
              <xsl:apply-templates/>
          </xsl:element>
        </xsl:template>

  <!-- Vikas Rohilla : Template to match the org -->
  <xsl:template match="org[child::remotelink][parent::contrib[parent::comm:info]]">
    <xsl:apply-templates select="@*|node()"/>
  </xsl:template>
      
</xsl:stylesheet>